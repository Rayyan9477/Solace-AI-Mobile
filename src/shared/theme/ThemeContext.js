/**
 * Theme Context for Solace AI Mental Health App
 * Re-exports from ThemeProvider for backward compatibility
 */

// Re-export everything from the ThemeProvider
export {
  ThemeProvider,
  ThemeContext,
  useTheme,
  lightTheme,
  darkTheme,
  therapeuticColors,
} from './ThemeProvider';
